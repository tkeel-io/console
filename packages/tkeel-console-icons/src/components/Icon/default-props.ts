const defaultProps = {
  mode: 'dark',
  size: 16,
  color: '',
  className: '',
  style: null,
};

const twoToneDefaultProps = {
  ...defaultProps,
  twoToneColor: '',
};

export { defaultProps, twoToneDefaultProps };
