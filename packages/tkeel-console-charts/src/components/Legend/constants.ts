const ALIGN: 'left' | 'center' | 'right' = 'right';
const VERTICAL_ALIGN: 'top' | 'middle' | 'bottom' = 'top';

export const DEFAULT_PROPS = {
  align: ALIGN,
  verticalAlign: VERTICAL_ALIGN,
  iconSize: 6,
  iconType: 'circle',
  wrapperStyle: { top: 0, right: 0 },
};
