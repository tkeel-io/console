const primaryColors = ['#2580FF', '#E9F2FF', '#CEE3FF', '#005DE0'];

const colors = {
  primary: primaryColors[0],

  brand: {
    50: primaryColors[1],
    200: primaryColors[2],
    500: primaryColors[0],
    700: primaryColors[3],
  },
};

export default colors;
