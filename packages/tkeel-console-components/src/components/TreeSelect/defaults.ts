import { DEFAULT_PROPS as DEFAULT_TREE_PROPS } from '../Tree/defaults';
import { TreeSelectProps } from './types';

export const DEFAULT_PREFIX_CLS = 'rc-tree-select-tree';

export const DEFAULT_PROPS = DEFAULT_TREE_PROPS as Partial<TreeSelectProps>;
